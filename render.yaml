databases:
  - name: my_budget_app_db
    databaseName: my_budget_app_db
    user: budget_app_db_user

services:
  - type: web
    name: macronaunt
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name:  my_budget_app_db
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false